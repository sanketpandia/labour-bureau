# PostgreSQL Database Configuration
# Copy this to database.env and fill in actual production values
# IMPORTANT: Change all CHANGE_ME values to strong, unique passwords

# PostgreSQL Configuration
POSTGRES_DB=infinite
POSTGRES_USER=ieuser
# POSTGRES_PASSWORD: Generate a strong random password (e.g., openssl rand -base64 32)
POSTGRES_PASSWORD=CHANGE_ME
