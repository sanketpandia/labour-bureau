# Redis Cache Configuration
# Copy this to cache.env and fill in actual production values
# IMPORTANT: Change CHANGE_ME to a strong, unique password

# Redis Password
# REDIS_PASSWORD: Generate a strong random password (e.g., openssl rand -base64 32)
REDIS_PASSWORD=CHANGE_ME
