# Monitoring Stack Configuration
# Copy this to monitoring.env and fill in actual production values
# IMPORTANT: Change CHANGE_ME to a strong, unique password

# Grafana Configuration
# GRAFANA_ADMIN_PASSWORD: Admin password for Grafana web interface
# Generate a strong random password (e.g., openssl rand -base64 32)
GRAFANA_ADMIN_PASSWORD=CHANGE_ME
